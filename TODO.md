# Header Nav Bar - Text Size Responsiveness Fix

## Steps:
1. ✅ Create TODO.md with plan breakdown
2. Fix brand name span - change `text-2xl` to `text-xl md:text-2xl`
3. Fix desktop nav links - add `text-sm` class
4. Fix mobile menu link items - add `text-sm` class
5. Fix mobile sidebar "Explore" heading - change `text-2xl` to `text-xl`

